name: build-test
description: Build and run tests

runs:
  using: composite
  steps:
    - name: Build packages
      run: pnpm -r build
      shell: bash

    - name: Run type check and unit tests
      run: |
        pnpm --filter core test
      shell: bash
